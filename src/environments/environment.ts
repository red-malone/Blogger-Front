// environment.ts removed: now using dotenv-webpack for build-time env injection.
// Keep this file only if you need TS-based environments. Otherwise delete it.
export const environment = {
  production: false,
};
